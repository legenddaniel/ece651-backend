# You don't need to manually install packages. All packages will be installed inside docker. 
# Put package name and version here and rebuild the images.

Django==4.0.1
djangorestframework==3.13.1
django-model-utils==4.2.0
psycopg2==2.9.3
